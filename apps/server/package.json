{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.50",
    "@ai-sdk/mcp": "^2.0.44",
    "@camaleon/db": "workspace:*",
    "@camaleon/env": "workspace:*",
    "@camaleon/mcp-banorte": "workspace:*",
    "@camaleon/mcp-research": "workspace:*",
    "@camaleon/shared": "workspace:*",
    "@hono/mcp": "^0.3.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ai": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.12.27",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@camaleon/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
